All images in this folder are already web-optimized (max 1600px wide, under
250 KB each) and named to match what the site expects. Nothing more to do.

If you swap in new photos later: resize to max 1600px wide, save as JPG at
~80% quality (squoosh.app or tinypng.com), and keep the same filenames --
or update the matching <img src="..."> in the HTML if you add new ones.
Descriptive filenames like "fade-haircut-zagreb.jpg" help Google Image Search.
